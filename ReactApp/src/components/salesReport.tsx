export default function SalesReport() {
    // if(startDate === undefined || endDate === undefined || dataPoints === undefined) {
    //     return <span>No Data!</span>
    // }
    return (
      <div>
        <h1>Sales Report</h1>
        {/* <p>Start Date: {startDate}</p> */}
        {/* <p>End Date: {endDate}</p> */}
        {/* <ul>
          {dataPoints ? dataPoints.map((dataPoint, index) => (
            <li key={index}>
              <p>Time: {dataPoint.time}</p>
              <p>Price: {dataPoint.price}</p>
              <p>Quantity: {dataPoint.quantity}</p>
            </li>
          )) : "no data!"}
        </ul> */}
      </div>
    );
  }